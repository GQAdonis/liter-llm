// Generated by alef. Do not edit by hand.
// alef:hash:21024cafe8651c0d4bc8aef48ff51062b80d98d92cb2ad74c504b493a318f4b9
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
    "UnusedParameter",
)

package io.xberg.literllm.android

/**
 * Wire format for the chat completions `response_format` field.
 *
 * # Provider mapping
 *
 * - **OpenAI** (and OpenAI-compatible providers): emitted verbatim as
 *   `{"type": "json_schema", "json_schema": {...}}` per the
 *   chat-completions spec.
 *
 * - **Gemini / Vertex AI**: translated to
 *   `generationConfig.responseMimeType = "application/json"` and
 *   `generationConfig.responseSchema = <schema>`. The `name`,
 *   `description`, and `strict` fields are dropped — Gemini's
 *   structured-output API does not consume them.
 *
 * - **Anthropic**: no native JSON mode. A system instruction is
 *   prepended asking the model to respond with valid JSON.
 *   `strict` is advisory only; callers should still validate the
 *   returned JSON if the schema is load-bearing.
 */
@com.fasterxml.jackson.annotation.JsonTypeInfo(use = com.fasterxml.jackson.annotation.JsonTypeInfo.Id.NAME, property = "type", visible = false)
@com.fasterxml.jackson.annotation.JsonSubTypes(
    com.fasterxml.jackson.annotation.JsonSubTypes.Type(value = ResponseFormat.Text::class, name = "text"),
    com.fasterxml.jackson.annotation.JsonSubTypes.Type(value = ResponseFormat.JsonObject::class, name = "json_object"),
    com.fasterxml.jackson.annotation.JsonSubTypes.Type(value = ResponseFormat.JsonSchema::class, name = "json_schema")
)
@com.fasterxml.jackson.annotation.JsonIgnoreProperties(ignoreUnknown = true)
sealed class ResponseFormat {
    /** Plain text output (default). */
    @com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = ResponseFormatTextDeserializer::class)
    object Text : ResponseFormat()
    /** Output must be valid JSON object (no schema validation). */
    @com.fasterxml.jackson.databind.annotation.JsonDeserialize(using = ResponseFormatJsonObjectDeserializer::class)
    object JsonObject : ResponseFormat()
    /** Output must conform to the specified JSON schema. */
    data class JsonSchema(val jsonSchema: JsonSchemaFormat) : ResponseFormat()
}

private class ResponseFormatTextDeserializer : com.fasterxml.jackson.databind.deser.std.StdDeserializer<ResponseFormat.Text>(ResponseFormat.Text::class.java) {
    override fun deserialize(
        parser: com.fasterxml.jackson.core.JsonParser,
        ctx: com.fasterxml.jackson.databind.DeserializationContext,
    ): ResponseFormat.Text {
        parser.skipChildren()
        return ResponseFormat.Text
    }
}

private class ResponseFormatJsonObjectDeserializer : com.fasterxml.jackson.databind.deser.std.StdDeserializer<ResponseFormat.JsonObject>(ResponseFormat.JsonObject::class.java) {
    override fun deserialize(
        parser: com.fasterxml.jackson.core.JsonParser,
        ctx: com.fasterxml.jackson.databind.DeserializationContext,
    ): ResponseFormat.JsonObject {
        parser.skipChildren()
        return ResponseFormat.JsonObject
    }
}
