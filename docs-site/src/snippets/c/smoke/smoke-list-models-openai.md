---
id: fixture_c_smoke_list_models_openai
language: c
target: c
level: null
requires: []
side_effect: safe
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:031bf15f389c4e58336f22438bd0e8d1067d6c2b913b5015e3c8c069d6ec77e0 -->
<!-- To regenerate: alef e2e generate -->
<!-- To verify freshness: alef verify -->

List models against real OpenAI API

```c title="C"
#include <assert.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "liter_llm.h"

#ifndef ALEF_TEST_SKIP
#define ALEF_TEST_SKIP(reason) do { \
    fprintf(stderr, "skipped: %s\n", (reason)); \
    return EXIT_SUCCESS; \
} while (0)
#endif

int main(void) {
    const char* api_key = getenv("OPENAI_API_KEY");
    if (getenv("OPENAI_API_KEY") == NULL) { ALEF_TEST_SKIP("OPENAI_API_KEY not set"); }
    LITERLLMAlefHandle client = literllm_create_client(api_key, NULL, (uint64_t)-1, (uint32_t)-1, NULL);
    LITERLLMAlefHandle result = literllm_default_client_list_models(client);
    literllm_models_list_response_free(result);
    literllm_default_client_free(client);
    return EXIT_SUCCESS;
}
```
