# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c736807d5a7738a54c4c23791baf438f2e9162bf5e69d6e3ddd983bd86582c63
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: contract
defmodule E2e.ContractTest do
  use ExUnit.Case, async: false

  describe "binding_api_parity" do
    test "binding_api_parity" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/binding_api_parity")

      {:ok, result} =
        LiterLlm.defaultclient_chat_async(
          client,
          "{\"messages\":[{\"content\":\"Contract test\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}"
        )

      assert length(result.choices) == 1
      assert ((Enum.at(result.choices, 0).message.content && Enum.at(result.choices, 0).message.content.text) || "") == "OK"
      assert LiterLlm.FinishReason.wire_value(Enum.at(result.choices, 0).finish_reason) == "stop"
      assert result.usage.total_tokens == 6
      assert result.model == "gpt-4o"
    end
  end

  describe "contract_ocr" do
    test "contract_ocr" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/contract_ocr")

      {:ok, result} =
        LiterLlm.defaultclient_ocr_async(
          client,
          "{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/contract-test.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}"
        )

      refute is_nil(result)
    end
  end

  describe "contract_search" do
    test "contract_search" do
      {:ok, client} = LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/contract_search")
      {:ok, result} = LiterLlm.defaultclient_search_async(client, "{\"model\":\"brave/web-search\",\"query\":\"contract test query\"}")
      refute is_nil(result)
    end
  end
end
