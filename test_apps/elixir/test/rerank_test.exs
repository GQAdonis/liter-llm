# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:5e803c482f0c36f7feadc2a3321142e9c94e42e62e468bed523ea8c59ad6435a
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: rerank
defmodule E2e.RerankTest do
  use ExUnit.Case, async: false

  describe "edge_rerank_empty_query" do
    test "edge_rerank_empty_query" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_rerank_empty_query")

      {:ok, result} =
        LiterLlm.defaultclient_rerank_async(
          client,
          "{\"documents\":[\"Some document\",\"Another document\"],\"model\":\"rerank-v3.5\",\"query\":\"\"}"
        )

      refute is_nil(result)
      assert length(result.results) == 2
    end
  end

  describe "edge_rerank_single_doc" do
    test "edge_rerank_single_doc" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/edge_rerank_single_doc")

      {:ok, result} =
        LiterLlm.defaultclient_rerank_async(
          client,
          "{\"documents\":[\"Artificial intelligence is the simulation of human intelligence.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is AI?\"}"
        )

      refute is_nil(result)
      assert length(result.results) == 1
      assert Enum.at(result.results, 0).relevance_score > 0.9
    end
  end

  describe "error_rerank_auth_401" do
    test "error_rerank_auth_401" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_rerank_auth_401")

      assert {:error, __reason} =
               LiterLlm.defaultclient_rerank_async(client, "{\"documents\":[\"doc1\"],\"model\":\"rerank-v3.5\",\"query\":\"test\"}")

      # skipped: declared error variant 'Authentication' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "error_rerank_bad_request" do
    test "error_rerank_bad_request" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_rerank_bad_request")

      assert {:error, __reason} =
               LiterLlm.defaultclient_rerank_async(client, "{\"documents\":[\"doc1\"],\"model\":\"nonexistent-rerank\",\"query\":\"test\"}")

      # skipped: declared error variant 'BadRequest' not yet preserved as a distinct identity by this backend's generator
    end
  end

  # skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend

  describe "smoke_rerank_basic" do
    test "smoke_rerank_basic" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_rerank_basic")

      {:ok, result} =
        LiterLlm.defaultclient_rerank_async(
          client,
          "{\"documents\":[\"Machine learning is a subset of AI.\",\"The weather is sunny today.\",\"Deep learning uses neural networks.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is machine learning?\"}"
        )

      refute is_nil(result)
      assert length(result.results) == 3
      assert Enum.at(result.results, 0).relevance_score > 0.9
    end
  end

  describe "smoke_rerank_return_docs" do
    test "smoke_rerank_return_docs" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_rerank_return_docs")

      {:ok, result} =
        LiterLlm.defaultclient_rerank_async(
          client,
          "{\"documents\":[\"Rust is a systems programming language.\",\"Iron rusts when exposed to water.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is Rust?\",\"return_documents\":true}"
        )

      refute is_nil(result)
      assert length(result.results) == 2
      assert Enum.at(result.results, 0).document not in [nil, "", [], %{}]
      assert Enum.at(result.results, 0).relevance_score > 0.9
    end
  end

  describe "smoke_rerank_with_top_n" do
    test "smoke_rerank_with_top_n" do
      {:ok, client} =
        LiterLlm.create_client("test-key", base_url: (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/smoke_rerank_with_top_n")

      {:ok, result} =
        LiterLlm.defaultclient_rerank_async(
          client,
          "{\"documents\":[\"Python is a programming language.\",\"Cats are cute animals.\",\"Python was created by Guido van Rossum.\",\"The sun is a star.\"],\"model\":\"rerank-v3.5\",\"query\":\"What is Python?\",\"top_n\":2}"
        )

      refute is_nil(result)
      assert length(result.results) == 2
      assert Enum.at(result.results, 0).relevance_score > 0.9
    end
  end
end
