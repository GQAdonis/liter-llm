// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:3913bbb331d6fa484078f1ea2f602c4a02a0a0c458a0da29280fb9abc34b5238
// To regenerate: alef generate
// To verify freshness: alef verify

// E2e tests for category: files
package e2e_test

import (
	"encoding/json"
	"fmt"
	"os"
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	pkg "github.com/xberg-io/liter-llm/packages/go"
)

func Test_EdgeFileEmptyList(t *testing.T) {
	// List files when no files have been uploaded
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_file_empty_list"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.ListFiles(nil)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 0, "expected exactly 0 elements")
}

func Test_EdgeFileLargeUpload(t *testing.T) {
	// Upload a large file successfully
	var request pkg.CreateFileRequest
	if err := json.Unmarshal([]byte(`{"file":"eyJwcm9tcHQiOiAibGFyZ2UgdHJhaW5pbmcgZGF0YSJ9Cg==","filename":"large_training_data.jsonl","purpose":"fine-tune"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_file_large_upload"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.CreateFile(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.ID) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_ErrorFileAuth401(t *testing.T) {
	// 401 Unauthorized when listing files with invalid API key
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_file_auth_401"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.ListFiles(nil)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `Authentication`) && !strings.Contains(fmt.Sprintf("%T", err), `Authentication`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `Authentication`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_ErrorFileBadPurpose(t *testing.T) {
	// 400 Bad Request when uploading a file with invalid purpose
	var request pkg.CreateFileRequest
	if err := json.Unmarshal([]byte(`{"file":"data.jsonl","purpose":"invalid-purpose"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_file_bad_purpose"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.CreateFile(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `BadRequest`) && !strings.Contains(fmt.Sprintf("%T", err), `BadRequest`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `BadRequest`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_ErrorFileNotFound(t *testing.T) {
	// 404 Not Found when retrieving a nonexistent file
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_file_not_found"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.RetrieveFile(`file-nonexistent`)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `NotFound`) && !strings.Contains(fmt.Sprintf("%T", err), `NotFound`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `NotFound`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_SmokeCreateFile(t *testing.T) {
	// Upload a file for use with the API
	var request pkg.CreateFileRequest
	if err := json.Unmarshal([]byte(`{"file":"eyJwcm9tcHQiOiAiaGVsbG8ifQo=","filename":"training_data.jsonl","purpose":"fine-tune"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_create_file"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.CreateFile(request)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.ID) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_SmokeDeleteFile(t *testing.T) {
	// Delete an uploaded file
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_delete_file"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.DeleteFile(`file-abc123`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.ID) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Deleted != true {
		t.Errorf("equals mismatch: got %v", result.Deleted)
	}
}

func Test_SmokeFileContent(t *testing.T) {
	// Retrieve the content of an uploaded file
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_file_content"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.FileContent(`file-abc123`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result) == 0 {
		t.Errorf("expected non-empty value")
	}
}

func Test_SmokeListFiles(t *testing.T) {
	// List all uploaded files
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_list_files"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.ListFiles(nil)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 2, "expected exactly 2 elements")
}

func Test_SmokeRetrieveFile(t *testing.T) {
	// Retrieve metadata for an uploaded file
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_retrieve_file"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.RetrieveFile(`file-abc123`)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.ID) == 0 {
		t.Errorf("expected non-empty value")
	}
}
