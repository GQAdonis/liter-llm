// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fedbbef3ace545a3cc83dd862bea696d91c78b9dea0a88bda8e6ce6d52ad9a31
// To regenerate: alef generate
// To verify freshness: alef verify

// E2e tests for category: error-handling
package e2e_test

import (
	"encoding/json"
	"fmt"
	"os"
	"strings"
	"testing"

	pkg "github.com/xberg-io/liter-llm/packages/go"
)

func Test_AnthropicErrorAuth(t *testing.T) {
	// 401 Authentication error returned by the Anthropic API when the API key is invalid
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"anthropic/claude-3-5-sonnet-20241022"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/anthropic_error_auth"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `Authentication`) && !strings.Contains(fmt.Sprintf("%T", err), `Authentication`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `Authentication`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_Auth401(t *testing.T) {
	// 401 Unauthorized error when API key is invalid or missing
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/auth_401"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `Authentication`) && !strings.Contains(fmt.Sprintf("%T", err), `Authentication`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `Authentication`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_AzureErrorAuth(t *testing.T) {
	// Azure OpenAI returns a 401 Unauthorized error when the API key is missing or invalid — uses Azure's error envelope shape with code AccessDenied
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"azure/gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/azure_error_auth"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `Authentication`) && !strings.Contains(fmt.Sprintf("%T", err), `Authentication`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `Authentication`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_BadRequest400(t *testing.T) {
	// 400 Bad Request error when a parameter value is invalid
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4","temperature":5.0}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/bad_request_400"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `BadRequest`) && !strings.Contains(fmt.Sprintf("%T", err), `BadRequest`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `BadRequest`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_BedrockErrorAuth(t *testing.T) {
	// AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"bedrock/anthropic.claude-3-sonnet-20240229-v1:0"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/bedrock_error_auth"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `Authentication`) && !strings.Contains(fmt.Sprintf("%T", err), `Authentication`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `Authentication`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_ContentPolicyViolation(t *testing.T) {
	// 400 error when a request is rejected due to content policy
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Generate harmful content","role":"user"}],"model":"gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/content_policy_violation"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `ContentPolicy`) && !strings.Contains(fmt.Sprintf("%T", err), `ContentPolicy`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `ContentPolicy`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_ContextWindowExceeded(t *testing.T) {
	// 400 error when the prompt exceeds the model's maximum context length
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Very long prompt that exceeds the context window...","role":"user"}],"model":"gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/context_window_exceeded"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `ContextWindowExceeded`) && !strings.Contains(fmt.Sprintf("%T", err), `ContextWindowExceeded`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `ContextWindowExceeded`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_EmptyResponseBody(t *testing.T) {
	// 200 OK response with an empty JSON object body, missing required fields
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/empty_response_body"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_Forbidden403(t *testing.T) {
	// 403 Forbidden error when the API key does not have access to the requested resource
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/forbidden_403"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `Authentication`) && !strings.Contains(fmt.Sprintf("%T", err), `Authentication`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `Authentication`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_GatewayTimeout504(t *testing.T) {
	// 504 Gateway Timeout error when the upstream service times out
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/gateway_timeout_504"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `ServiceUnavailable`) && !strings.Contains(fmt.Sprintf("%T", err), `ServiceUnavailable`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `ServiceUnavailable`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_GithubCopilotErrorAuth(t *testing.T) {
	// 401 Authentication error returned by the GitHub Copilot API when the token is invalid or expired
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"github_copilot/gpt-4o"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/github_copilot_error_auth"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `Authentication`) && !strings.Contains(fmt.Sprintf("%T", err), `Authentication`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `Authentication`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_NotFound404(t *testing.T) {
	// 404 Not Found error when requesting a model that does not exist
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-99"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/not_found_404"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `NotFound`) && !strings.Contains(fmt.Sprintf("%T", err), `NotFound`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `NotFound`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_RateLimit429(t *testing.T) {
	// 429 Too Many Requests error when the rate limit is exceeded
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/rate_limit_429"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `RateLimited`) && !strings.Contains(fmt.Sprintf("%T", err), `RateLimited`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `RateLimited`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_ServerError500(t *testing.T) {
	// 500 Internal Server Error from the upstream API
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/server_error_500"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `ServerError`) && !strings.Contains(fmt.Sprintf("%T", err), `ServerError`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `ServerError`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_ServiceUnavailable502(t *testing.T) {
	// 502 Bad Gateway error when the upstream service is unavailable
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/service_unavailable_502"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `ServiceUnavailable`) && !strings.Contains(fmt.Sprintf("%T", err), `ServiceUnavailable`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `ServiceUnavailable`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_TimeoutError(t *testing.T) {
	// 408 Request Timeout error when the API request takes too long to complete
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/timeout_error"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `Timeout`) && !strings.Contains(fmt.Sprintf("%T", err), `Timeout`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `Timeout`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_VertexErrorAuth(t *testing.T) {
	// Google Vertex AI returns 401 Unauthorized when the OAuth2 token is missing, expired, or the service account lacks aiplatform.endpoints.predict permission — verifies the error is mapped to Authentication
	var request pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"vertex_ai/gemini-2.0-flash"}`), &request); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/vertex_error_auth"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Chat(request)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `Authentication`) && !strings.Contains(fmt.Sprintf("%T", err), `Authentication`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `Authentication`, err.Error(), err)
		}
	}
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}
