/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:29b7de19090abcb9e7adfe47bfaf95518fab55567f7173276a3f2a1ad8b5ecac
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: parity */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "liter_llm.h"
#include "test_runner.h"

void test_api_surface_parity(void) {
    /* Canonical API surface definition for cross-binding parity verification */
    LITERLLMAlefHandle chat_completion_request_handle = literllm_chat_completion_request_from_json("{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4o\"}");
    assert(chat_completion_request_handle != 0 && "failed to build request");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    char base_url[1024];
    snprintf(base_url, sizeof(base_url), "%s/fixtures/api_surface_parity", mock_base);
    LITERLLMAlefHandle client = literllm_create_client("test-key", base_url, (uint64_t)-1, (uint32_t)-1, NULL);
    assert(client != 0 && "failed to create client");
    LITERLLMAlefHandle result = literllm_default_client_chat(client, chat_completion_request_handle);
    assert(result != 0 && "expected call to succeed");
    literllm_chat_completion_response_free(result);
    literllm_chat_completion_request_free(chat_completion_request_handle);
    literllm_default_client_free(client);
}
