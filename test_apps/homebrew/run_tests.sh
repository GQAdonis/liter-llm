#!/usr/bin/env bash
# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:60607cebc73555c92248e13c7a657943e801829953095edd6dcbcffbb23bc021
# To regenerate: alef e2e generate
# To verify freshness: alef verify
# Tests the Homebrew CLI formula.
set -euo pipefail

VERSION='2.1.0'
TAP='xberg-io/homebrew-tap'
CLI_FORMULA='liter-llm'
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

PASS=0
FAIL=0

pass() {
  echo "PASS: $1"
  PASS=$((PASS + 1))
}
fail() {
  echo "FAIL: $1 — $2" >&2
  FAIL=$((FAIL + 1))
}

# Trust and install formulae.
brew trust "$TAP" || true
brew bundle install --file="$SCRIPT_DIR/Brewfile"

# Test: version.
_command_version='$CLI_FORMULA --version | grep -Fxq "$CLI_FORMULA $VERSION" && printf "%s" "version-match"'
_output_version=$(eval "$_command_version" 2>&1 || true)
_expected_version='version-match'
if [[ "$_output_version" == *"$_expected_version"* ]]; then
  pass 'version'
else
  fail 'version' "expected '$_expected_version' in output, got: $_output_version"
fi

# Test: help.
_command_help='$CLI_FORMULA --help'
_output_help=$(eval "$_command_help" 2>&1 || true)
_expected_help='Usage'
if [[ "$_output_help" == *"$_expected_help"* ]]; then
  pass 'help'
else
  fail 'help' "expected '$_expected_help' in output, got: $_output_help"
fi

echo ""
echo "Results: $PASS passed, $FAIL failed"
[ "$FAIL" -eq 0 ]
